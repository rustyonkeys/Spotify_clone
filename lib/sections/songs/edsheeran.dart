import 'package:flutter/material.dart';

class EdSheeran extends StatelessWidget {
  const EdSheeran({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Ed Sheeran"),),
    );
  }
}

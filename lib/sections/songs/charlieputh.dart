import 'package:flutter/material.dart';

class CharliePuth extends StatelessWidget {
  const CharliePuth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Charlie Puth"),),
    );
  }
}

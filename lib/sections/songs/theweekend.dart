import 'package:flutter/material.dart';

class TheWeekend extends StatelessWidget {
  const TheWeekend({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("The Weekend"),),
    );
  }
}

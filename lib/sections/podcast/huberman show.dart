import 'package:flutter/material.dart';

class HubermanShow extends StatelessWidget {
  const HubermanShow({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Huberman Show"),),
    );
  }
}
